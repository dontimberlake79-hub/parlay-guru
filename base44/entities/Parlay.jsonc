{
  "name": "Parlay",
  "type": "object",
  "properties": {
    "title": {
      "type": "string"
    },
    "sport": {
      "type": "string",
      "enum": [
        "NFL",
        "NBA",
        "MLB",
        "NHL",
        "NCAAF",
        "NCAAB",
        "MLS",
        "UFC",
        "Tennis",
        "Golf",
        "Other"
      ]
    },
    "date": {
      "type": "string",
      "description": "ISO date string for when this parlay is for"
    },
    "scheduledAt": {
      "type": "string",
      "description": "ISO datetime for scheduled post time"
    },
    "parlayType": {
      "type": "string",
      "enum": [
        "quick_hit",
        "main_slate",
        "power_parlay"
      ],
      "default": "main_slate",
      "description": "Quick Hit = 2 legs, Main Slate = 3 legs, Power Parlay = 4-5 legs"
    },
    "legs": {
      "type": "array",
      "items": {
        "type": "object"
      },
      "description": "Array of leg objects"
    },
    "totalOdds": {
      "type": "string"
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "won",
        "lost",
        "push"
      ],
      "default": "pending"
    },
    "published": {
      "type": "boolean",
      "default": false
    },
    "tier": {
      "type": "string",
      "enum": [
        "free",
        "pro",
        "vip"
      ],
      "default": "pro"
    },
    "capperId": {
      "type": "string"
    },
    "notes": {
      "type": "string"
    },
    "publishedAt": {
      "type": "string"
    }
  },
  "required": [
    "title",
    "sport",
    "date"
  ]
}